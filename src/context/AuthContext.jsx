// src/context/AuthContext.jsx - FIXED VERSION
import { createContext } from 'react';

// Create the context
const AuthContext = createContext();

// Export the context as default and named export
export default AuthContext;
export { AuthContext };